Autocomplete.java implements autocomplete for a given set of n terms (where terms are strings and associated non-negative weights). 
Given a prefix, the autocomplete class finds all queries that begin with the prefix and returns them in reverse
order of weights. 

Term.java  represents an autocomplete term of a query string and associated weight. Supports comparing terms using three different comparators:
lexicographic order, descending order by weight, and lexicographic order with just the first r characters. 

When binary searching a sorted array that contains more than one key equal to the search key, the client may want to know the index of either the first or the last such key. Accordingly, implement the following API:

